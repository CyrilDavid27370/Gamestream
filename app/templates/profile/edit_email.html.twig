{% extends 'base.html.twig' %}

{% block title %}Modifier mon email - GameStream{% endblock %}

{% block body %}
    <main class="container py-4">
        <div class="card shadow rounded-4 overflow-hidden border border-primary" style="max-width: 500px; margin: 0 auto;">
            <div class="card-body p-4">
                <h2 class="text-primary text-center mb-4">
                    <i class="bi bi-envelope"></i> Modifier mon email
                </h2>

                {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                    <div class="mb-3">
                        {{ form_label(form.email) }}
                        {{ form_widget(form.email) }}
                        <div class="text-danger mt-1">
                            {{ form_errors(form.email) }}
                        </div>
                    </div>

                    <div class="text-center">
                        <button type="submit" class="btn btn-primary rounded-pill px-4 w-100">
                            <i class="bi bi-check-lg"></i> Enregistrer
                        </button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>

        <div class="text-center mt-4">
            <a href="{{ path('app_profile') }}" class="btn btn-outline-primary rounded-pill">
                <i class="bi bi-arrow-left"></i> Retour au profil
            </a>
        </div>
    </main>
{% endblock %}
